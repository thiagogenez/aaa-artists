@AGENTS.md

# Claude Code adapter

`AGENTS.md` is the shared project contract. Read `.context/README.md` when the ignored local
Obsidian vault exists, and keep Claude-only preferences, permissions, MCP configuration, and
auto-memory in `~/.claude/` or `.claude/` instead of duplicating shared project facts here.
